package com.omrital.smartrecorder.core.Planning;

public class PlanningFile {

    //  1  create an infinity service - DONE
    //  2  start record a voice
    //  3  stop record a voice
    //  4  save the record to file
    //  5  create simple UI - two tabs (1 - start/stop/recording, 2 - records list, title and length)
    //  6  handle volume up & volume down clicks
    //  7  save *recording_now* FLAG in storage
    //  8  when service killed (onTaskRemoved called) => if *recording_now* - stop the current recording & start new recording in (onStartCommand)
}
